//Variables
H = 0;
A = 0;
X = x;
Y = y;
Cadencia = 4;
Device = -1;
doubletap = -1;

// Apuntar hacia el dedo (test)
/*
for( i = 0; i < 4; i++ ) {
    if(device_mouse_check_button( i, mb_left )) and (device_mouse_x_to_gui(i) > (320+120)) and (device_mouse_y_to_gui(i) > 200) {
        Device = i;
        x = device_mouse_x_to_gui(i);
        y = device_mouse_y_to_gui(i);
        X = device_mouse_x_to_gui(i);
        Y = device_mouse_y_to_gui(i);
    }
}

/* */
/*  */
