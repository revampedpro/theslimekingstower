// Create lazer
    event_inherited();
alarm[0] = 1;
    audio_stop_sound(snd_Laser);
    audio_play_sound(snd_Laser, 0, 0);
    audio_sound_gain(snd_Laser, 1, 200);

// Set depth
LAngulo = 0;
Parent = -1;
x2 = x;
y2 = y;
