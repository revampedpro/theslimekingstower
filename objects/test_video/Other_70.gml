// Give the player money
/*
var name = ds_map_find_value(async_load, "currname");
var amount = ds_map_find_value(async_load, "curramount");
switch(string(name)) {
    case "Gold Coins": 
        global.Monedas_General += real(amount);
        ini_open( "save.ini" );
        ini_write_real("S","MG",global.Monedas_General);
        ini_close();
        break;
}
*/

