/// @description Entity collision check
/*
if (can_collide)
{
    if (parent != other.id)
    {
        if (other.invincible < 0)
        {
            other.take_damage = damage;
            other.vx += (vx/8)*pushback;
            other.vy += (vy/8)*pushback;
        }
        instance_destroy();
    }
}



/* */
/*  */
